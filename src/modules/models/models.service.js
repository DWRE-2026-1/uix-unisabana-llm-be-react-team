import { notImplemented } from "../../utils/not-implemented.js";

export const modelsService = {
  async list() {
    return notImplemented("modelsService", "list()");
  },
  async setDefaultProvider(_provider) {
    return notImplemented("modelsService", "setDefaultProvider(provider)");
  }
};
