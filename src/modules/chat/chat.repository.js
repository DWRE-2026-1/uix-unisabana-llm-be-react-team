import { notImplemented } from "../../utils/not-implemented.js";

export const chatRepository = {
  async saveExchange(_payload) {
    return notImplemented("chatRepository", "saveExchange(payload)");
  }
};
