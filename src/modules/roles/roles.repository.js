import { notImplemented } from "../../utils/not-implemented.js";

export const rolesRepository = {
  async findAll() {
    return notImplemented("rolesRepository", "findAll()");
  },
  async create(_payload) {
    return notImplemented("rolesRepository", "create(payload)");
  }
};
